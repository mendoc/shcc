services:
  api:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: shcc-api-dev
    ports:
      - "8080:8080"
    volumes:
      - .:/app
    env_file:
      - .env
    environment:
      - AIR_CONF=.air.toml
